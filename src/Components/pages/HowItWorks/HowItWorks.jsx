import React from 'react';
import { Outlet } from 'react-router-dom';

const HowItWorks = () => {
    return (
        <div>
            <Outlet />
        </div>
    );
};

export default HowItWorks;